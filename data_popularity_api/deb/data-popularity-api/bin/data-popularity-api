#!/bin/bash
#. /usr/share/python/data-popularity-api/bin/activate

RUN_DIR=/var/run/data-popularity-api

if [ ! -d "$RUN_DIR" ]
then
    mkdir -p "$RUN_DIR"
fi


exec /usr/share/python/data-popularity-api/bin/gunicorn -c /etc/skygrid/data-popularity-api.gunicorn.config app.app:app
